{
  "name": "wasm-par-mq-web-tests",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "webpack build ./index.js --mode production -o dist --output-filename index.js && cp index.html dist/ && cp ../js/coordinator.js dist/",
    "server": "serve --config ../serve.json dist/"
  },
  "devDependencies": {
    "serve": "^14.2.5",
    "webpack": "^5.105.0",
    "webpack-cli": "^5.1.4"
  }
}
